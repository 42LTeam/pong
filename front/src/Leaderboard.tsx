import React from 'react';

type Props = {
    children?: React.ReactNode
}

export const Leaderboard = (props: Props) => {
    return (
        <div>
            Pagre to see the leaderboard
        </div>
    )
}

export default Leaderboard;