import React from 'react';

type Props = {
    children?: React.ReactNode
}

export const HomePageReal = (props: Props) => {
    return (
        <div>
            This is the home page
        </div>
    )
}

export default HomePageReal;